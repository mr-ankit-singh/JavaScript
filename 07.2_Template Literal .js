//#__Template Literal (Template string) __#

for (let num =1 ; num <= 10 ; num++){
    let tableof = 12;
    // console.log(tableof + " * " + num + " = " + tableof * num);
    console.log(`${tableof} * ${num} = ${tableof * num}`); // Easy mtd to use a variable
    
}