/*
#__ Event Propagation __#

   -The Event Propagation mode determines in which order the element recieve the event
   -Event propagation is a mechanism that defines how events propagate or travel through the DOM tree  
     to arrive at its target and what happens to it afterward.
   
   -'Event bubbling' and 'capturing' are two way of event propogation in the HTML DOM API 
   -When an event occurs in an element inside another element ,and both element have registered a handler for that event

   -The event propagation mode determine in which order the elements recieve thr event
    
    
*/