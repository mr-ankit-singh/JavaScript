/*
#__ Conditional (Ternary ) operator __#

   - The Conditional (Ternary ) operator is the only javascript operator that takes three operands.
   - SyntaX:
        variablename = (condition) ? value1 : value2
*/

var myAge = 18
var result =(myAge >= 18) ? "you are eligible yo vote" : "you ae not eligible to vota";
console.log(result)